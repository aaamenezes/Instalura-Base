import styled from 'styled-components'

export const Button = styled.button`
  padding: 12px 26px;
  border: 0;
  font-weight: bold;
  opacity: 1;
  cursor: pointer;

  &:hover, &:focus {
    opacity: .5;
  }
`;